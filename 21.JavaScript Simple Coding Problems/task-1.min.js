function getLarger(first, second, third) {
    var larger = Math.max(first, second, third);
    return larger
}
// var maxValue = getLarger(110, 220, 300);
// console.log(maxValue)


function getLower(value1, value2, value3, value4, value5) {
    var lower = Math.min(value1, value2, value3, value4, value5);
    return lower
}
var minValue = getLower(12, 33, 11, 10, 8);
console.log(minValue)
